server:
  port: 8094

spring:
  application:
    name: user-centric-server

app:
  id: user-centric-server #apollo配置中心应用名
apollo:
  meta: http://192.168.111.129:8080 #apollo地址
  bootstrap:
    enabled: true #是否启用apollo, 不使用apollo 请剔除该包
    namespaces: application.yml